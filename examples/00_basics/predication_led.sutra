; Sutra v1.3 — predykcja na b0/b1 + LED-y

start:
    move r0, 10
    move r1, 3
    cmp.gt b0, r0, r1

    (b0)  move @led0, high
    (!b0) move @led0, low

    cmp.lt b1, r0, r1
    (b1)  move @led1, high
    (!b1) move @led1, low

    wait 27000000
    jump start
