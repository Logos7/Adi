# FPGA tools

Small repository-local helpers for FPGA system composition and build preparation.

Initial helper:

```text
compose_constraints.py
```

It concatenates Gowin `.cst` fragments into one generated `.cst` file.
