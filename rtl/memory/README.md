# Memory RTL

Reusable memory blocks and memory wrappers.

Examples:

- inferred block RAM,
- initialized ROM,
- dual-port RAM wrappers,
- vendor-specific BRAM wrappers,
- small memory buses.

Processor-specific memory maps should stay near the core or the system wrapper.
