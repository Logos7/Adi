# UART RTL

Reusable UART building blocks.

Examples:

- UART RX,
- UART TX,
- baud-rate tick generators,
- simple stream adapters.

Agni-specific UART bootloader protocol should stay in `cores/agni/` or a system-specific wrapper.
