# Common RTL

Small generic modules used by many systems.

Examples:

- reset synchronizers,
- edge detectors,
- simple FIFOs,
- counters,
- debounce logic,
- clock-domain crossing helpers.

These modules must not depend on one board, one vendor, or one processor core.
