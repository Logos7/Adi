# Gowin build area

This directory is reserved for generated or tool-owned Gowin build files for the modular Agni UART bootloader system.

The current safe first generated artifact is:

```text
generated/tang_nano_20k_uart.cst
```
