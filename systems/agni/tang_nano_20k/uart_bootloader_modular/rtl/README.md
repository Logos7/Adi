# System RTL

System-specific top-level wrappers belong here.

For this first manifest-only step, the system points at the existing stable top-level:

```text
systems/agni/tang_nano_20k/uart_bootloader/rtl/agni_top.v
```

A later commit can add a new local top-level once the composition model is proven.
