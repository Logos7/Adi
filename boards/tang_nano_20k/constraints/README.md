# Tang Nano 20K constraints

Board constraints are physical board data: pins, IO standards, clocks, LEDs, UART, HDMI, and other external interfaces.

Reusable logic must not live here. Shared hardware logic belongs in `rtl/`. Complete system wiring belongs in `systems/`.
